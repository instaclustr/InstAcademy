meta {
  name: Attach default ingest pipeline
  type: http
  seq: 11
}

put {
  url: {{baseUrl}}/my-sparse-neural-index/_settings
  body: json
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
{"index": {"default_pipeline": "nlp-ingest-pipeline"}}
}
