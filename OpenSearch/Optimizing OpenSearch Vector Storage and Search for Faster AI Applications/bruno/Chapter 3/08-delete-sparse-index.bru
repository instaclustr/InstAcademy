meta {
  name: Delete my-sparse-neural-index (optional)
  type: http
  seq: 8
}

docs {
Only needed if re-running — index mappings are mostly immutable.
}

delete {
  url: {{baseUrl}}/my-sparse-neural-index
  body: none
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}
