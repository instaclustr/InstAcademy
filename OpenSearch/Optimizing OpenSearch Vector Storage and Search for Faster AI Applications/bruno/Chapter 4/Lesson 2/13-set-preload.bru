meta {
  name: Set index.store.preload
  type: http
  seq: 13
}

put {
  url: {{baseUrl}}/bookstore-rag/_settings
  body: json
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
{"index": {"store": {"preload": ["vec", "vem"]}}}
}
