meta {
  name: Register sparse encoding model
  type: http
  seq: 16
}

docs {
Optional (Lesson 2-3). Poll with 05, save model_id to sparseModelId. Chapter 3 reuses this exact model - keep it deployed and skip Chapter 3 requests 03-06.
}

post {
  url: {{baseUrl}}/_plugins/_ml/models/_register
  body: json
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

body:json {
{
  "name": "amazon/neural-sparse/opensearch-neural-sparse-encoding-v1",
  "version": "1.0.1",
  "model_group_id": "{{modelGroupId}}",
  "model_format": "TORCH_SCRIPT"
}
}
